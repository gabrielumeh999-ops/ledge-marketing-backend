{
  "name": "ledge-marketing",
  "version": "1.0.0",
  "description": "Email marketing SaaS for Whop",
  "main": "Backend/server.js",
  "scripts": {
    "start": "node Backend/server.js",
    "build": "cd frontend && npm install && npm run build",
    "render-build": "npm install && cd Backend && npm install && cd ../frontend && npm install && npm run build && cd .."
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": ["whop", "email", "marketing", "saas"],
  "author": "",
  "license": "ISC"
}
